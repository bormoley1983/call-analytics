[pytest]
markers =
    integration: integration tests requiring external services (deselect with '-m "not integration"')
    postgres: tests requiring Postgres database (deselect with '-m "not postgres"')
timeout = 60
timeout_func_only = true
