# -*- coding: utf-8 -*-
"""Golden baseline responses for report endpoints.

Each JSON file captures the expected structure for a key report endpoint.
Timestamps and non-deterministic fields are represented as placeholders.

Files:
- overall_report.json — GET /reports/overall (empty dataset)
- managers_report.json — GET /reports/managers (empty dataset)
- customers_report.json — GET /reports/customers (empty dataset)
- keywords_report.json — GET /reports/keywords (empty dataset)
"""
